defmodule Day00Test do
  use ExUnit.Case

  test "first problem" do
    inputs = ~S"""
    """
    assert Day00.solve_1(inputs) == []
  end

  test "second problem" do
    inputs = ~S"""
    """
    assert Day00.solve_2(inputs) == []
  end
end
